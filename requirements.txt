# ============================================================
# LinkedIn Auto-Publisher Pipeline — requirements.txt
# Stack: LangGraph + FastAPI + PostgreSQL + LinkedIn + LLM
# Python: 3.12+
# Last updated: April 2026
# ============================================================

# ------------------------------------------------------------
# CORE FRAMEWORK
# ------------------------------------------------------------
fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.3
pydantic-settings==2.9.1
python-dotenv==1.1.0
httpx==0.28.1

# ------------------------------------------------------------
# LANGGRAPH + LANGCHAIN
# ------------------------------------------------------------
langgraph==0.3.34
langgraph-checkpoint-postgres==2.0.21
langchain-core>=0.3.63,<0.4
langchain-anthropic==0.3.15
langchain-openai==0.3.16

# ------------------------------------------------------------
# LLM PROVIDERS
# ------------------------------------------------------------
anthropic>=0.52.0,<1.0.0
openai==1.77.0
tiktoken==0.9.0

# ------------------------------------------------------------
# DATABASE — PostgreSQL
# ------------------------------------------------------------
sqlalchemy==2.0.41
alembic==1.15.2
asyncpg==0.30.0                  # async PostgreSQL driver
psycopg2-binary==2.9.10          # sync driver (Alembic + checkpointer)
psycopg[binary,pool]==3.2.6      # psycopg3 for LangGraph checkpointer

# ------------------------------------------------------------
# CLOUD STORAGE — AWS S3 / GCS / MinIO
# ------------------------------------------------------------
boto3==1.38.0                    # AWS S3 + S3-compatible (MinIO)
botocore==1.38.0
google-cloud-storage==3.1.0      # GCS (optional, env-toggled)

# ------------------------------------------------------------
# CONTENT EXTRACTION
# ------------------------------------------------------------
pypdf==5.4.0                     # PDF text extraction
python-docx==1.1.2               # DOCX extraction
markdown==3.8                    # Markdown parsing
python-magic==0.4.27             # MIME type detection
pillow==11.2.1                   # Image processing
chardet==5.2.0                   # Encoding detection for text files

# ------------------------------------------------------------
# LINKEDIN OAUTH2 + API
# ------------------------------------------------------------
requests==2.32.3
requests-oauthlib==2.0.0
oauthlib==3.2.2

# ------------------------------------------------------------
# SCHEDULING
# ------------------------------------------------------------
apscheduler==3.11.0
pytz==2025.2
python-dateutil==2.9.0.post0

# ------------------------------------------------------------
# APPROVAL WORKFLOW — GOOGLE SHEETS + SLACK
# ------------------------------------------------------------
google-api-python-client==2.165.0
google-auth==2.40.1
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.1
slack-sdk==3.35.0

# ------------------------------------------------------------
# OBSERVABILITY — LOGGING + METRICS + TRACING
# ------------------------------------------------------------
structlog==25.3.0
prometheus-client==0.21.1
opentelemetry-api==1.33.1
opentelemetry-sdk==1.33.1
opentelemetry-instrumentation-fastapi==0.54b1
opentelemetry-instrumentation-sqlalchemy==0.54b1
opentelemetry-exporter-otlp==1.33.1
python-json-logger==3.3.0

# ------------------------------------------------------------
# LOCAL FOLDER WATCHER (dev/test mode)
# ------------------------------------------------------------
watchdog==6.0.0

# ------------------------------------------------------------
# SECURITY + VALIDATION
# ------------------------------------------------------------
bleach==6.2.0                    # HTML sanitisation (prompt injection defence)
cryptography==44.0.3
python-jose[cryptography]==3.5.0 # JWT (future API auth)
passlib[bcrypt]==1.7.4

# ------------------------------------------------------------
# CLI + UTILITIES
# ------------------------------------------------------------
typer==0.15.3                    # CLI commands (seed, retry, status)
rich==14.0.0                     # Pretty terminal output
click==8.1.8
tabulate==0.9.0

# ------------------------------------------------------------
# TESTING
# ------------------------------------------------------------
pytest==8.3.5
pytest-asyncio==0.26.0
pytest-cov==6.1.0
pytest-mock==3.14.0
pytest-env==1.1.5
factory-boy==3.3.3               # Test data factories
faker==37.1.0
httpx==0.28.1                    # AsyncClient for FastAPI testing (already above)
respx==0.22.0                    # Mock httpx requests in tests
moto[s3]==5.1.3                  # Mock AWS S3 in tests

# ------------------------------------------------------------
# CODE QUALITY (dev only — also in pre-commit)
# ------------------------------------------------------------
ruff==0.11.8
black==25.1.0
mypy==1.15.0
pre-commit==4.2.0
types-requests==2.32.0.20250328
types-python-dateutil==2.9.0.20241206
types-pytz==2025.2.0.20250326
types-bleach==6.1.0.20240331
types-tabulate==0.9.0.20241207

# ------------------------------------------------------------
# FASTMCP — LinkedIn MCP Server (Phase 4)
# ------------------------------------------------------------
fastmcp==2.5.1

# ------------------------------------------------------------
# ALEMBIC RUNTIME DEP
# ------------------------------------------------------------
mako==1.3.10
